version: '3.8'

services:
  # Redis — in-memory store for seat locking and caching
  redis:
    image: redis:7-alpine
    container_name: voyago-redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  # Uncomment below in Step 6 when deploying the full app via Docker
  # backend:
  #   build: ./backend
  #   ports:
  #     - "5000:5000"
  #   env_file: ./backend/.env
  #   depends_on:
  #     - redis
  #
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend
